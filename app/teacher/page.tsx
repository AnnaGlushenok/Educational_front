import mainStyle from "../mainstyle.module.css";

export const metadata = {
    title: 'Главная учитель'
}

export default function Teacher() {
    return (
        <main className={mainStyle.main}>
            teacher Main
        </main>
    )
}