import mainStyle from "../mainstyle.module.css";

export const metadata = {
    title: 'Создать контрольную'
}

export default function CreateControl() {
    return (
        <main className={mainStyle.main}>
            CreateTask
        </main>
    )
}